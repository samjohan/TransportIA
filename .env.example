# Copy this file to `.env` (gitignored, never commit real values) for local
# `docker compose up` — docker-compose.yml reads it automatically.

# BotFather-issued token for the driver-facing Telegram bot. Leave unset to
# skip it — the telegram-bot service logs an error and exits without
# affecting the rest of the stack.
TELEGRAM_BOT_TOKEN=
